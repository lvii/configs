#!/bin/sh
cmd=$(echo -e "shutdown\nreboot\nlogout" | dmenu -nb "#00050A" -nf "#5E687D" -sb "#00050A" -sf "#878F96")
case $cmd in
	shutdown)
		sudo shutdown -h now
		;;
	reboot)
		sudo shutdown -r now
		;;
	logout)
		sudo killall X
		;;
esac
