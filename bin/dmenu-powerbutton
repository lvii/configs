#!/bin/sh
cmd=$(echo -e "shutdown\nreboot\nlogout" | dmenu -nb "#140c0b" -nf "#94928f" -sb "#140c0b" -sf "#cdcdcd")
case $cmd in
	shutdown)
		sudo shutdown -h now
		;;
	reboot)
		sudo shutdown -r now
		;;
	logout)
		sudo killall X
		;;
esac
